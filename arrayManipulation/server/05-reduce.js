const totals = [1, 2, 3, 4];

// For

for(let index = 0; index < Array.length; index ++ ) {
    const element = array[index];
}
console.log(sum);

// Reduce

const rta = totals.reduce((sum, item) => sum + element, 0)
console.log('rta', rta);